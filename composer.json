{
    "name": "efoft/query-builder",
    "type": "library",
    "description": "Helper classes allowing to generate native syntax queries to databases",
    "require": {
        "php": ">=5.3.0"
    },
    "autoload": {
        "psr-4": {
            "QueryBuilder\\": "src"
        }
    }
}
